# Copy to .env and fill in. .env is gitignored.
# Used by docker-compose.yml on the staging host (maverick.dzsec.net).

MYSQL_ROOT_PASSWORD=change-me-strong-root-password
MYSQL_PASSWORD=change-me-strong-wp-db-password
WORDPRESS_DB_PASSWORD=change-me-strong-wp-db-password
WORDPRESS_TABLE_PREFIX=nrm_
